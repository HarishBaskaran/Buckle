import Button from "../hb_components/storybook/button";
import Input from "../hb_components/storybook/input";

const Application = () => {
  return (
    <div className="flex flex-col gap-3">
      <p>Harish</p>
      <Button />
      <Input />
    </div>
  );
};

export default Application;
